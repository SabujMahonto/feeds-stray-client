export const metadata = {
  title: "FeedStray | Consumer Connector Dashboard",
};

export default function ConsumerConnector({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
