export const metadata = {
  title: "FeedStray | Provider Connector Dashboard",
};

export default function ProviderConnector({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
